# Configuração Padrão (Local H2 para evitar erros de timeout)
spring.datasource.url=jdbc:h2:mem:matchpet;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=create-drop

# Server Configuration
server.port=8080

# Para usar o MySQL do Railway, execute com:
# mvn spring-boot:run -Dspring-boot.run.profiles=railway
